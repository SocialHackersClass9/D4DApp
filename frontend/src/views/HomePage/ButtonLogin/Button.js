import React from 'react';
import Button from 'react-bootstrap/Button';


const button = () => (
    <div>
    <Button variant="info">Sign Up</Button>
    <Button variand="primary">Login</Button>
    </div>
)

export default button;